# Category-A
The purpose of this project is for practical Examination for Set-A

# If Python is not installed
# Run this in the Codespaces terminal:

sudo apt update
sudo apt install python3 -y

# Check if Python is installed:

python3 --version

# Install SQLITE3

sudo apt update
sudo apt install sqlite3 -y

# Check SQLite3

sqlite3 --version


# ETL Practical Exam Category A
# Topic: Load unstructured TXT data into SQLite3 using Python

# Student Task:
# Complete only the following SQLite3 parts:

# 1. CREATE TABLE
# 2. INSERT INTO
# 3. SELECT FROM

# The extraction and transformation code is already provided.

